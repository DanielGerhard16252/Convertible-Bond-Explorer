# AI features only; manual Bloomberg search works without an OpenAI key.
OPENAI_API_KEY=
# Optional model override:
# OPENAI_MODEL=
# Standalone CSV utilities only (desktop uses Bloomberg).
# Paths are relative to the project root or executable directory:
# BOND_CSV_PATH=data/search_demo.csv
# OPTION_CSV_PATH=data/option_demo.csv
